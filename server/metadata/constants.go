package metadata

const (
	// AuthorizationKey is the key used for authorization in metadata.
	AuthorizationKey = "authorization"

	// RefreshTokenKey is the key used for refresh token in metadata.
	RefreshTokenKey = "x-refresh-token"

	// AccessTokenKey is the key used for access token in metadata.
	AccessTokenKey = "x-access-token"
)
